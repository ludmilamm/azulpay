<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/regular</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light"/>

    <style name="Button" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:fontFamily">@font/semi_bold</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="colorButtonNormal">@color/persianGreen</item>
        <item name="android:colorButtonNormal">@color/persianGreen</item>
    </style>

    <style name="ButtonBlueBig" parent="Button">
        <item name="android:paddingBottom">15dp</item>
        <item name="android:paddingTop">15dp</item>
        <item name="android:paddingStart">17dp</item>
        <item name="android:paddingEnd">17dp</item>
    </style>

    <style name="ButtonBlueSmall" parent="Button">
        <item name="android:padding">10dp</item>
    </style>

    <style name="ImageViewRoundBordered">
        <item name="android:background">@drawable/shape_blue_circle</item>
        <item name="android:padding">3dp</item>
    </style>

</resources>
